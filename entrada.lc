int x = 3;
boolean y = 1 > 5;
final z = true;

begin
    while 3 > 2
    begin
        if x == y
        begin
            write, "Hello ";
            write, "World!";
         end
         if x >= 3 begin write, x; end
         write, y;
    end
end