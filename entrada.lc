int x = 3;
boolean y = 1 > 5;
final z = true;

begin
    while 3 > 2
        begin
        if x >= 3 write, x; else write, "x < 3";
        if x == y
        begin
            write, "Hello ";
            write, "World!";
        end
        else
        begin
            write, "Olá ";
            write, "Mundo!";
        end
        write, y;
    end
end